Merge the test suite from git branch with_tests (commit e4e62da, 19 tests) into the current src/core.rs and src/models.rs on this branch. Then check git branch shex_with_tests (commit 69edd2b, 16 tests, diverged separately from an earlier commit) via 'git show shex_with_tests:src/core.rs' for any test cases or fixes not present in with_tests's version, and fold those in too. Do not change any non-test production logic while doing this merge -- if a test fails against current core.rs because the production code genuinely changed since the test was written, fix the test to match current intended behavior rather than reverting the production code, and note in a code comment why. Run cargo test after to confirm everything passes.
In src/core.rs, when a candidate patch fails to apply (both in evaluate_mode_3 and evaluate_modes_1_2_4), extend the Diagnostic message to include the real current original_code content, capped at 4000 characters, appended after the existing diffy error string -- so a caller can see why the hunk didn't match without re-reading original_code separately. Follow the ~4000-char precedent described in INTELLIGENCE_TRANSFER_RUCHAT_TO_PATCH_REFINER.md section A.1. Add or update a unit test asserting the diagnostic message contains a truncated snippet of original_code on an apply failure.
Update README.md's Quickstart section (the "Build the project" and "Run the CLI" steps) to include the --features cli flag in every cargo build/cargo run example that builds or runs the patch-refiner binary, since clap and the [[bin]] target are gated behind the optional cli feature (see Cargo.toml) and the current Quickstart commands silently build only the library without it. Do not change any other section of README.md.
