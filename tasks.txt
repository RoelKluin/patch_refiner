In src/core.rs, extend the pre-parse diff-repair step (the one that already fixes missing leading spaces on context lines) to also recompute wrong @@ hunk-header line counts from the actual hunk body, per ROADMAP_BACKLOG.md section 3.3 and .claude/rules/patch-application.md: never use the model-written @@ -start,count +start,count @@ numbers to decide anything -- when they're wrong, recompute the correct start/count values by counting the actual context/added/removed lines in that hunk's body and rewrite the header, rather than rejecting or trusting the original numbers. This must not change hunk semantics (only the header's line-count numbers), and must not touch the leading-space repair already in place. Add unit tests: a diff with a wrong hunk-header count that should now parse and apply correctly after repair, and confirm an already-correct diff's header is left byte-for-byte unchanged.
In src/core.rs, reduce non-load-bearing code duplication per ROADMAP_BACKLOG.md section 3.6: cargo dupes found duplication in default_commands-style per-language boilerplate and in config-merge/default-filling boilerplate (e.g. repeated unwrap_or_default()/field-by-field merge patterns for RefinementConfig's sub-configs). Consolidate these via small pure-function helpers or data-driven tables where it's genuinely mechanical duplication. Do NOT touch ChangeSet, handle_part_inner, compute_distance, or any of the hypothesis-tracking lexer state machine in this file -- per .claude/rules/compute-distance-lexer.md that code is fragile-by-design and explicitly out of scope for this cleanup, regardless of how much duplication cargo dupes reports there. Run cargo test after and confirm no behavior changed (all existing tests still pass with no test modifications needed -- if a test would need to change, that's a sign the refactor touched behavior, not just structure, and should be reverted instead).
